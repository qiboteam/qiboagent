## `qibo_core` generation

This code contains an example of the qibo_core module written in Rust with Python bindings.

The code was generated by an LLM and contains the basic objects found in Qibo (Gate and Circuit).

The core is written in Rust, and the qibo_python directory contains the bindings created with PyO3, along with some simple Python tests to verify its correct functionality.